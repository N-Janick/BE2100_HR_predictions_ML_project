# script used to transform the cleaned datasets into a dataset that 
# includes all of the features. May need to take in multipled cleaned 
# data sets, but should output a single csv stored in data/processed folder